const path = require('path');

module.exports = {
    'config': path.join(process.cwd(), 'config', 'config.json'),
    'models-path': path.join(process.cwd(), 'src', 'models'),
    'seeders-path': path.join(process.cwd(), 'src', 'seeders'),
    'migrations-path': path.join(process.cwd(), 'src', 'migrations')
};