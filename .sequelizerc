const path = require('path');

module.exports = {
    // Config file is in the root 'config' directory
    'config': path.resolve('config', 'config.json'),
    // Models are inside the 'src' directory
    'models-path': path.resolve('src', 'models'),
    // Assume seeders are also inside the 'src' directory
    'seeders-path': path.resolve('src', 'seeders'),
    // Assume migrations are also inside the 'src' directory
    'migrations-path': path.resolve('src', 'migrations')
};