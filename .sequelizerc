const path = require('path');

module.exports = {
    // Go up one level from 'src' to find the 'config' directory
    'config': path.join('..', 'config', 'config.json'),
    // Paths relative to the 'src' directory
    'models-path': 'models',
    'seeders-path': 'seeders',
    'migrations-path': 'migrations'
};